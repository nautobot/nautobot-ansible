---
##
##
### PYNAUTOBOT_METADATA_TYPE
##
##
- name: "1 - Create metadata type with minimum information"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: TopSecretInfo
    data_type: text
    content_types:
      - dcim.device
    state: present
  register: test_one

- name: "1 - ASSERT"
  assert:
    that:
      - test_one is changed
      - test_one['diff']['before']['state'] == "absent"
      - test_one['diff']['after']['state'] == "present"
      - test_one['metadata_type']['name'] == "TopSecretInfo"
      - test_one['metadata_type']['data_type'] == "text"
      - test_one['metadata_type']['content_types'] == ['dcim.device']
      - test_one['msg'] == "metadata_type TopSecretInfo created"

- name: "2 - Create metadata type with minimum information idempotent"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: TopSecretInfo
    data_type: text
    content_types:
      - dcim.device
    state: present
  register: test_two

- name: "2 - ASSERT"
  assert:
    that:
      - not test_two['changed']
      - test_two['metadata_type']['name'] == "TopSecretInfo"
      - test_two['metadata_type']['data_type'] == "text"
      - test_two['metadata_type']['content_types'] == ['dcim.device']
      - test_two['msg'] == "metadata_type TopSecretInfo already exists"

- name: "3 - Update metadata type"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: TopSecretInfo
    description: The topest secretest info
    data_type: text
    content_types:
      - dcim.device
      - dcim.location
    state: present
  register: test_three

- name: "3 - ASSERT"
  assert:
    that:
      - test_three is changed
      - test_three['diff']['after']['description'] == "The topest secretest info"
      - test_three['diff']['after']['content_types'] == ['dcim.device', 'dcim.location']
      - test_three['metadata_type']['name'] == "TopSecretInfo"
      - test_three['metadata_type']['description'] == "The topest secretest info"
      - test_three['metadata_type']['data_type'] == "text"
      - test_three['metadata_type']['content_types'] == ['dcim.device', 'dcim.location']
      - test_three['msg'] == "metadata_type TopSecretInfo updated"

- name: "4 - Update idempotent"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: TopSecretInfo
    description: The topest secretest info
    data_type: text
    content_types:
      - dcim.device
      - dcim.location
    state: present
  register: test_four

- name: "4 - ASSERT"
  assert:
    that:
      - not test_four['changed']
      - test_four['metadata_type']['name'] == "TopSecretInfo"
      - test_four['msg'] == "metadata_type TopSecretInfo already exists"

- name: "5 - Create metadata type with all params"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: TopSecretInfoToo
    description: The topest secretest info too
    data_type: text
    content_types:
      - dcim.device
      - dcim.location
    state: present
  register: test_five

- name: "5 - ASSERT"
  assert:
    that:
      - test_five is changed
      - test_five['metadata_type']['name'] == "TopSecretInfoToo"
      - test_five['metadata_type']['description'] == "The topest secretest info too"
      - test_five['metadata_type']['data_type'] == "text"
      - test_five['metadata_type']['content_types'] == ['dcim.device', 'dcim.location']
      - test_five['msg'] == "metadata_type TopSecretInfoToo created"

- name: "6 - Create metadata type with all params idempotent"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: TopSecretInfoToo
    description: The topest secretest info too
    data_type: text
    content_types:
      - dcim.device
      - dcim.location
    state: present
  register: test_six

- name: "6 - ASSERT"
  assert:
    that:
      - not test_six['changed']
      - test_six['metadata_type']['name'] == "TopSecretInfoToo"
      - test_six['metadata_type']['description'] == "The topest secretest info too"
      - test_six['metadata_type']['data_type'] == "text"
      - test_six['metadata_type']['content_types'] == ['dcim.device', 'dcim.location']
      - test_six['msg'] == "metadata_type TopSecretInfoToo already exists"

- name: "7 - Delete"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: TopSecretInfoToo
    state: absent
  register: test_seven

- name: "7 - ASSERT"
  assert:
    that:
      - test_seven is changed
      - test_seven['diff']['before']['state'] == "present"
      - test_seven['diff']['after']['state'] == "absent"
      - test_seven['msg'] == "metadata_type TopSecretInfoToo deleted"

- name: "8 - Delete idempotent"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: TopSecretInfoToo
    state: absent
  register: test_eight

- name: "8 - ASSERT"
  assert:
    that:
      - not test_eight['changed']
      - test_eight['metadata_type'] == None
      - test_eight['msg'] == "metadata_type TopSecretInfoToo already absent"

##
## M2M metadata_choices tests
##
- name: "9 - Create metadata type with metadata_choices"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: SelectMetadata
    data_type: multi-select
    content_types:
      - dcim.device
    metadata_choices:
      objects:
        - value: "Meta A"
    state: present
  register: test_nine

- name: "9 - ASSERT"
  assert:
    that:
      - test_nine is changed
      - test_nine['diff']['before']['state'] == "absent"
      - test_nine['diff']['after']['state'] == "present"
      - test_nine['metadata_type']['name'] == "SelectMetadata"
      - test_nine['metadata_type']['data_type'] == "multi-select"
      - test_nine['diff']['after']['metadata_choices'] | length == 1
      - test_nine['msg'] == "metadata_type SelectMetadata created"

- name: "10 - Duplicate create with metadata_choices idempotent"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: SelectMetadata
    data_type: multi-select
    content_types:
      - dcim.device
    metadata_choices:
      objects:
        - value: "Meta A"
    state: present
  register: test_ten

- name: "10 - ASSERT"
  assert:
    that:
      - not test_ten['changed']
      - test_ten['msg'] == "metadata_type SelectMetadata already exists"

- name: "11 - Merge second metadata choice"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: SelectMetadata
    metadata_choices:
      objects:
        - value: "Meta B"
    state: present
  register: test_eleven

- name: "11 - ASSERT"
  assert:
    that:
      - test_eleven is changed
      - test_eleven['diff']['before']['metadata_choices'] | length == 1
      - test_eleven['diff']['after']['metadata_choices'] | length == 2

- name: "12 - Duplicate merge idempotent"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: SelectMetadata
    metadata_choices:
      objects:
        - value: "Meta B"
    state: present
  register: test_twelve

- name: "12 - ASSERT"
  assert:
    that:
      - not test_twelve['changed']
      - test_twelve['msg'] == "metadata_type SelectMetadata already exists"

- name: "14 - Delete second metadata choice"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: SelectMetadata
    metadata_choices:
      state: delete
      objects:
        - value: "Meta B"
    state: present
  register: test_fourteen

- name: "14 - ASSERT"
  assert:
    that:
      - test_fourteen is changed
      - test_fourteen['diff']['before']['metadata_choices'] | length == 2
      - test_fourteen['diff']['after']['metadata_choices'] | length == 1

- name: "15 - Duplicate delete idempotent"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: SelectMetadata
    metadata_choices:
      state: delete
      objects:
        - value: "Meta B"
    state: present
  register: test_fifteen

- name: "15 - ASSERT"
  assert:
    that:
      - not test_fifteen['changed']
      - test_fifteen['msg'] == "metadata_type SelectMetadata already exists"

- name: "16 - Replace with second metadata choice"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: SelectMetadata
    metadata_choices:
      state: replace
      objects:
        - value: "Meta B"
    state: present
  register: test_sixteen

- name: "16 - ASSERT"
  assert:
    that:
      - test_sixteen is changed
      - test_sixteen['diff']['before']['metadata_choices'] | length == 1
      - test_sixteen['diff']['after']['metadata_choices'] | length == 1

- name: "17 - Duplicate replace idempotent"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: SelectMetadata
    metadata_choices:
      state: replace
      objects:
        - value: "Meta B"
    state: present
  register: test_seventeen

- name: "17 - ASSERT"
  assert:
    that:
      - not test_seventeen['changed']
      - test_seventeen['msg'] == "metadata_type SelectMetadata already exists"

- name: "18 - Delete SelectMetadata metadata type"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: SelectMetadata
    state: absent
  register: test_eighteen

- name: "18 - ASSERT"
  assert:
    that:
      - test_eighteen is changed
      - test_eighteen['diff']['before']['state'] == "present"
      - test_eighteen['diff']['after']['state'] == "absent"
      - test_eighteen['msg'] == "metadata_type SelectMetadata deleted"

- name: "19 - Delete SelectMetadata idempotent"
  networktocode.nautobot.metadata_type:
    url: "{{ nautobot_url }}"
    token: "{{ nautobot_token }}"
    name: SelectMetadata
    state: absent
  register: test_nineteen

- name: "19 - ASSERT"
  assert:
    that:
      - not test_nineteen['changed']
      - test_nineteen['metadata_type'] == None
      - test_nineteen['msg'] == "metadata_type SelectMetadata already absent"
