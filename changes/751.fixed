Added root `requirements.txt` (generated from `pyproject.toml` at build time) so `ansible-builder` can resolve `pynautobot`, `requests`, `netutils`, and `aiohttp` when building Execution Environments.
