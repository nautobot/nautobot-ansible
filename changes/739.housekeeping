Lowered `requires_ansible` floor in `meta/runtime.yml` from `>=2.18.0` to `>=2.17.0` so the collection passes the `ansible-lint meta-runtime[unsupported-version]` check run by automation-hub's galaxy-importer.
